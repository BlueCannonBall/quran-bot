# Polybuild Configuration

When making changes to `Polybuild.toml`, do NOT manually edit the generated makefiles (such as `Makefile` or `.polybuild.mk`). Instead, run the `polybuild` command in the terminal to automatically regenerate the build files.
